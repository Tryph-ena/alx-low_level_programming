#include <stdio.h>
/**
 *main - entry
 *prints alphabets with lowercase, then uppercase
 *return: 0
 */ int n;
int main(void);
{
char c;
char C;
{
for (c = 'a'; c <= 'z'; c++)
putchar(c);
for (C = 'A'; C <= 'Z'; C++)
putchar(C);
putchar('\n');
}
return (0);
}
